{
  "name": "catcoding-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "npm run gen:sitemap",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "gen:sitemap": "node scripts/gen-sitemap.mjs",
    "audit:lighthouse": "node scripts/lighthouse-baseline.mjs",
    "ci": "npm run check && npm run build",
    "ci:quality": "npm run ci && LH_SKIP_BUILD=1 LH_MIN_PERFORMANCE=90 npm run audit:lighthouse"
  },
  "dependencies": {
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "http-server": "^14.1.1",
    "lighthouse": "^13.1.0",
    "typescript": "^5.9.3"
  }
}
